post:
  summary: Logout
  description: Expires the given authorization token.

  tags:
    - Security
    -
  parameters:
    - name: Content-Type
      in: header
      schema:
        type: string
      example: application/json

  requestBody:
    content:
      application/json:
        schema:
          type: object
          example:
            sessid: <user token>

  responses:
    '200':
      description: OK
